# Dependencies — each package installs its own; none are committed.
node_modules/
**/node_modules/

# Local databases (CRDT log, snapshots, index, auth, blobs) + WAL sidecars
*.db
*.db-wal
*.db-shm

# Tooling caches & logs
.vite/
**/.vite/
*.log

# Build output (e.g. the bundled VS Code extension: vscode/out/extension.cjs)
**/out/

# Env / secrets
.env
**/.env*
